# Shared rofi key-bindings for waybar submenus.
# Source this file; use "${ROFI_VIM_FLAGS[@]}" when invoking rofi.
ROFI_VIM_FLAGS=(
    -kb-row-up       "k,Up"
    -kb-row-down     "j,Down"
    -kb-accept-entry "l,Return"
    -kb-cancel       "h,Escape"
)
